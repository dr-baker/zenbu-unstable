// Side-effect CSS imports (e.g. `allotment/dist/style.css`) are
// handled by the host Vite server at runtime. Same declaration the
// app plugin uses (`plugins/app/src/renderer/css-modules.d.ts`).
declare module "*.css"
